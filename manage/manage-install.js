const resources = require('../lib/resources-tools.js')

module.exports = (args, options, logger) => {

    let templatesDir = resources.getDir(args.resource)

    logger.info(`Preparing to install new ${args.resource}`)



}
